<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="hezeu.jsj.dao.AdminDao">
    <select id="getAdminByUP" resultType="hezeu.jsj.domain.Admin">
        select * from admin_tab where username=#{userName} and password=#{password}
    </select>
</mapper>