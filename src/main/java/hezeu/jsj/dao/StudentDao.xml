<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="hezeu.jsj.dao.StudentDao">
    <select id="getStudentByUP" resultType="hezeu.jsj.domain.Student">
       select * from student_tab where stuNo=#{stuNo} and password=#{password}
    </select>
</mapper>